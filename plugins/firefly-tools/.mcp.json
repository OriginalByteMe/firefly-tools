{
  "mcpServers": {
    "firefly": {
      "command": "uvx",
      "args": ["--python", ">=3.10", "--from", "git+https://github.com/OriginalByteMe/claude-open-finance", "firefly-mcp"],
      "cwd": "${CLAUDE_PLUGIN_ROOT}"
    }
  }
}
