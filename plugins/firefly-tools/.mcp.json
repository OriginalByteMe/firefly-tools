{
  "mcpServers": {
    "firefly": {
      "command": "uvx",
      "args": ["--from", "git+https://github.com/OriginalByteMe/claude-open-finance", "firefly-mcp"],
      "cwd": "${CLAUDE_PLUGIN_ROOT}"
    }
  }
}
